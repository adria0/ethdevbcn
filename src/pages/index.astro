---
import { loadEvents } from "../lib/events";

const allEvents = loadEvents();
const today = new Date();
today.setHours(0, 0, 0, 0); // Reset time to midnight for accurate date comparison

const nextEvents = allEvents.filter((e) => new Date(e.date) >= today);
const previousEvents = allEvents.filter((e) => new Date(e.date) < today);

const linkifyDetails = (value: string) =>
    value
        .replace(
            /\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g,
            '<a class="details-link" style="color:#fff;text-decoration:underline;" href="$2" target="_blank" rel="noreferrer">$1</a>',
        )
        .replace(
            /(https?:\/\/[^\s<]+)/g,
            (match: string, _p1: string, offset: number, source: string) => {
                const preceding = source.slice(0, offset);
                if (preceding.endsWith('href="')) {
                    return match;
                }
                return (
                    '<a class="details-link" style="color:#fff;text-decoration:underline;" href="' +
                    match +
                    '" target="_blank" rel="noreferrer">' +
                    match +
                    "</a>"
                );
            },
        );
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Ethereum Developers Barcelona</title>
        <link
            rel="stylesheet"
            href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700&display=swap"
        />
        <link
            rel="stylesheet"
            href="https://fonts.googleapis.com/css2?family=Kode+Mono:wght@400..700&display=swap"
        />
        <style>
            body {
                font-family: "Kode Mono", system-ui, sans-serif;
                margin: 0;
                padding: 0;
                background-color: black;
                color: white;
            }
            .header-wrapper {
                background-color: black;
                width: 100%;
                padding: 56px 0 16px;
            }
            .header-image {
                width: clamp(200px, 28vw, 420px);
                height: auto;
                display: block;
                margin: 0 auto;
            }
            .content {
                padding: 24px;
            }
            header {
                display: flex;
                gap: 12px;
                align-items: baseline;
                flex-wrap: wrap;
            }
            section {
                margin-top: 32px;
            }
            h1,
            h2 {
                font-family: "Orbitron", system-ui, sans-serif;
                letter-spacing: 0.08em;
                text-transform: uppercase;
            }
            h2 {
                margin: 16px 0;
                font-size: 1.5rem;
            }
            .grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 16px;
                margin-top: 16px;
            }
            .card {
                border: 1px solid #444;
                border-radius: 12px;
                padding: 14px 14px 12px;
                background-color: #1a1a1a;
            }
            .meta {
                font-family: "Kode Mono", system-ui, sans-serif;
                opacity: 0.8;
                font-size: 0.95rem;
                margin: 6px 0 10px;
            }
            .details {
                font-family: "Kode Mono", system-ui, sans-serif;
                font-size: 0.75rem;
                color: #fff;
                margin: 0 0 10px;
            }
            .no-events {
                font-family: "Kode Mono", system-ui, sans-serif;
                color: #fff;
                opacity: 0.7;
                margin: 24px 0;
            }
            .title {
                font-family: "Kode Mono", system-ui, sans-serif;
                font-weight: 650;
                font-size: 1.05rem;
                margin: 0;
            }
            a {
                text-decoration: none;
                color: #4da6ff;
            }
            a:hover {
                text-decoration: underline;
            }
            .details-link,
            .details-link:visited,
            .details-link:hover,
            .details-link:focus {
                color: #fff !important;
                text-decoration: underline;
            }
            .pill {
                display: inline-block;
                padding: 2px 8px;
                border-radius: 999px;
                border: 1px solid #444;
                font-size: 0.8rem;
                opacity: 0.9;
            }
            .event-link {
                display: inline-block;
                margin-top: 12px;
                color: #ffd166;
                font-weight: 600;
                font-family: "Kode Mono", system-ui, sans-serif;
            }
        </style>
    </head>
    <body>
        <div class="header-wrapper">
            <img src="/images/header.jpeg" alt="Header" class="header-image" />
        </div>
        <div class="content">
            <header>
                <h1 style="margin:0;">Events</h1>
                <span class="pill">{allEvents.length} total</span>
            </header>

            {
                nextEvents.length > 0 ? (
                    <section>
                        <h2>Next Events</h2>
                        <div class="grid">
                            {nextEvents.map((e) => (
                                <div class="card">
                                    <p class="title">
                                        {e.url ? (
                                            <a
                                                href={e.url}
                                                target="_blank"
                                                rel="noreferrer"
                                            >
                                                {e.title}
                                            </a>
                                        ) : (
                                            e.title
                                        )}
                                    </p>
                                    <div class="meta">
                                        <div>
                                            {e.date}
                                            {e.time ? ` · ${e.time}` : ""}
                                            {e.timezone
                                                ? ` (${e.timezone})`
                                                : ""}
                                        </div>
                                        <div>
                                            {[e.venue, e.city, e.country]
                                                .filter(Boolean)
                                                .join(" · ")}
                                        </div>
                                    </div>
                                    {e.details ? (
                                        <p
                                            class="details"
                                            set:html={linkifyDetails(e.details)}
                                        />
                                    ) : null}
                                    {e.source ? (
                                        <div class="pill">{e.source}</div>
                                    ) : null}
                                    {e.link_url ? (
                                        <a
                                            class="event-link"
                                            href={e.link_url}
                                            target="_blank"
                                            rel="noreferrer"
                                        >
                                            {e.link_text ?? "Event link"}
                                        </a>
                                    ) : null}
                                </div>
                            ))}
                        </div>
                    </section>
                ) : (
                    <p class="no-events">No events</p>
                )
            }

            {
                previousEvents.length > 0 && (
                    <section>
                        <h2>Previous Events</h2>
                        <div class="grid">
                            {previousEvents.map((e) => (
                                <div class="card">
                                    <p class="title">
                                        {e.url ? (
                                            <a
                                                href={e.url}
                                                target="_blank"
                                                rel="noreferrer"
                                            >
                                                {e.title}
                                            </a>
                                        ) : (
                                            e.title
                                        )}
                                    </p>
                                    <div class="meta">
                                        <div>
                                            {e.date}
                                            {e.time ? ` · ${e.time}` : ""}
                                            {e.timezone
                                                ? ` (${e.timezone})`
                                                : ""}
                                        </div>
                                        <div>
                                            {[e.venue, e.city, e.country]
                                                .filter(Boolean)
                                                .join(" · ")}
                                        </div>
                                    </div>
                                    {e.details ? (
                                        <p
                                            class="details"
                                            set:html={linkifyDetails(e.details)}
                                        />
                                    ) : null}
                                    {e.source ? (
                                        <div class="pill">{e.source}</div>
                                    ) : null}
                                    {e.link_url ? (
                                        <a
                                            class="event-link"
                                            href={e.link_url}
                                            target="_blank"
                                            rel="noreferrer"
                                        >
                                            {e.link_text ?? "Event link"}
                                        </a>
                                    ) : null}
                                </div>
                            ))}
                        </div>
                    </section>
                )
            }
        </div>
    </body>
</html>
